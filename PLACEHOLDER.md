# oss（同期待ちの初期状態）

このリポジトリは公開用ミラーです。まだコードは同期されていません。

コードは開発用リポジトリ `oss-dev` の `public/` から、GitHub Actions
（Sync public → oss）によって Pull Request として反映されます。

初回同期の Pull Request がマージされると、`README.md` や `src/` などの実際の
コードがこのリポジトリに現れます。このプレースホルダは、その後に削除して
構いません（`public/` には含まれないため同期では消えません）。
